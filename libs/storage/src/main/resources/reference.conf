solution {
  storage {
    protocol {
      mode = h2
      h2 {
        url = "jdbc:h2:mem:toy;DB_CLOSE_DELAY=-1"
        username = "toy"
        password = "toy"
      }
      postgres {
        url = "jdbc:postgresql://localhost:5432/toy"
        schema = "toy"
        username = "toy"
        password = "toy"
      }
    }
    mapping {
      mode = spring_data
    }
  }
}
