bezmen {
  storage {
    protocol {
      mode = h2
      h2 {
        url = "jdbc:h2:mem:bezmen;DB_CLOSE_DELAY=-1"
        username = "sa"
        password = "sa"
      }
      postgres {
        url = "jdbc:postgresql://localhost:5432/bezmen"
        username = "bezmen"
        password = "bezmen"
      }
    }
    mapping {
      mode = spring_data
    }
  }
}
